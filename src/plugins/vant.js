import Vue from 'vue'
import { Button, Icon, Cell, Form, Field, Switch, Dialog } from 'vant'
import 'vant/lib/index.css'

Vue.use(Button).use(Icon).use(Cell).use(Form).use(Field).use(Switch).use(Dialog)
